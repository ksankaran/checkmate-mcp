# Checkmate MCP Server Configuration

# Port to run the MCP server on
PORT=3003

# Checkmate API URL (use 127.0.0.1, not localhost, for Node.js fetch compatibility)
CHECKMATE_URL=http://127.0.0.1:8000
